# Exemplo de variáveis de ambiente para o projeto
# Copie este arquivo para .env e preencha os valores antes de rodar.

# URL pública do site (usada para gerar URLs absolutas em feeds)
SITE_URL=http://localhost

# Chave opcional para proteger o endpoint de feeds (api/exportProperties.php)
FEED_API_KEY=troque_esta_chave_por_uma_secreta

# Banco de dados
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=rr_imoveis

# Ambiente: development | production
ENVIRONMENT=development
